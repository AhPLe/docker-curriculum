# untested - which folder, expecting to be in the same folder

set FLASK_APP=hello
set FLASK_ENV=development
flask run --host=0.0.0.0
#incorrect: flask run


for ubuntu: export instead of set

docker build -t hickorydock1/dockerpa2 .
docker run -p 8888:5000 hickorydock1/dockerpa2